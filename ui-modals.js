/* ui-modals.js — split into per-cluster files. This file is intentionally empty;
   it stays in the script load order as a marker for older bookmarks. The modal
   implementations now live in:

     ui-modal-copy.js          — Copy modal (Clone editor)
     ui-modal-text-change.js   — Text-change / swirl / exchange / exchange-control / creature-type
     ui-modal-mutate.js        — Mutate modal (CR 702.140)
     ui-modal-bestow.js        — Bestow modal (CR 702.102)
     ui-modal-equip.js         — Equip / Reconfigure / Fortify / Imprint modals
     ui-modal-snapshot.js      — Board snapshot popup (camera button)
*/
